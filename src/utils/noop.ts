// eslint-disable-next-line @typescript-eslint/no-empty-function
export const NOOP = () => {};

export const NOOP_RETURN = (arg: any) => {
  return arg;
};

export const NOOP_RETURNS = (...args: any[]) => {
  return args;
};
